<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yc.resta.mapper.UserMapper">
	<select id="findUser" parameterType="Users" resultType="Users">
		select * from useres where u_id=#{u_id} and upwd=#{upwd}
	</select>
	
	<insert id="addUser" parameterType="Users" >
		insert into useres(u_id,uname,upwd,umail,unickname,uphone,usex,uaddr,uheadimg) 
		values(seq_u_id.nextval,#{uname},#{upwd},#{umail},#{unickname},#{uphone},#{usex},#{uaddr},#{uheadimg})
	</insert>
</mapper>